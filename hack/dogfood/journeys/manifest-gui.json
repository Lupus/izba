{
  "feature": "izba desktop app — Manifest tab (izba.yml diff/promote/export)",
  "journeys": [
    {
      "journey_id": "manifest-open-tab-in-sync",
      "modality": "gui",
      "tier": "smoke",
      "rationale": "A user who already has a valid izba.yml in their workspace and creates the sandbox from it should see the Manifest tab confirm there is nothing to reconcile. This is the tab's simplest, most common state and the swarm's cheapest way to exercise the manifest_truth oracle's happy path.",
      "source": {
        "kind": "spec",
        "ref": "docs/superpowers/specs/2026-07-11-gui-manifest-dogfood-design.md §3.1 Manifest tab; ManifestTab.tsx BANNER_TEXT.in_sync"
      },
      "seed_files": {
        "izba.yml": "apiVersion: izba.dev/v1alpha1\nkind: Sandbox\nspec:\n  image: alpine:3.20\n"
      },
      "steps": [
        {
          "intent": "type {workspace} into the Workspace field, 'manifest-sync-demo' into the Name field, and 'alpine:3.20' into the Image field, then click Create to create a new sandbox from this workspace",
          "expect": "a sandbox named manifest-sync-demo appears in the sandbox list"
        },
        {
          "intent": "open the manifest-sync-demo sandbox's detail view, click its Manifest tab, and click Refresh",
          "expect": "the Manifest tab shows 'In sync — izba.yml and managed settings match.'",
          "core": true
        }
      ]
    },
    {
      "journey_id": "manifest-seeded-drift-promote",
      "modality": "gui",
      "tier": "smoke",
      "rationale": "The headline manifest workflow: someone edits izba.yml (here simulating that edit as a precondition of the step, since the GUI Actor has no file-editing surface of its own), the app must show the drift, and Promote must apply it. This is the harness's ground-truth-verified decisive assertion — manifest_diff's reported state must match `izba diff`'s. The GUI creates the sandbox STOPPED and the seeded drift is an image change (alpine:3.20 -> alpine:3.21): the core promote gate (crates/izba-core/src/manifest/promote.rs:254) refuses an image-class change without restart authorization REGARDLESS of run state, so ManifestTab now shows a stopped-case checkbox (`RESTART_CHECKBOX_LABEL_STOPPED_IMAGE`) that must be ticked before Promote is even clickable. Before that fix (dogfood acceptance run 2, PF1) this journey's final step was a dead end: the dialog had no control that could satisfy the gate and Promote always failed underneath a contradictory 'applies on the next start' note.",
      "source": {
        "kind": "spec",
        "ref": "docs/superpowers/specs/2026-07-11-gui-manifest-dogfood-design.md §3.2 Promote in the app; ManifestTab.tsx BANNER_TEXT.repo_ahead + Promote dialog"
      },
      "seed_files": {
        "izba.yml": "apiVersion: izba.dev/v1alpha1\nkind: Sandbox\nspec:\n  image: alpine:3.20\n"
      },
      "steps": [
        {
          "intent": "type {workspace} into the Workspace field, 'manifest-drift-demo' into the Name field, and 'alpine:3.20' into the Image field, then click Create to create a new sandbox from this workspace",
          "expect": "a sandbox named manifest-drift-demo appears in the sandbox list"
        },
        {
          "intent": "open the manifest-drift-demo sandbox's detail view, click its Manifest tab, and click Refresh to see whether izba.yml has any changes not yet applied",
          "expect": "the Manifest tab shows 'izba.yml has changes not yet applied. Review below, then Promote.' with a table row showing the image changing from alpine:3.20 to alpine:3.21",
          "core": true,
          "seed_files": {
            "izba.yml": "apiVersion: izba.dev/v1alpha1\nkind: Sandbox\nspec:\n  image: alpine:3.21\n"
          }
        },
        {
          "intent": "click 'Promote…' on the Manifest tab, tick the 'Start the sandbox to apply the image change (the scratch disk is kept, not reset)' checkbox, then confirm by clicking 'Promote' in the dialog that appears",
          "expect": "the dialog reports 'Promoted 1 change(s).' and 'Sandbox was started to apply the change.', and, after closing it and refreshing, the Manifest tab shows 'In sync — izba.yml and managed settings match.'"
        }
      ]
    },
    {
      "journey_id": "manifest-missing-manifest-guidance",
      "modality": "gui",
      "tier": "smoke",
      "rationale": "A workspace with no izba.yml at all is the common first-time state (nothing was ever exported); the tab must guide the user to create one rather than show a bare error or an empty table. NOT marked core: real-bridge.js records a manifest_diff invoke-log digest only on a successful (ok) response (app/dogfood/real-bridge.js __dfHandleMessage), and ManifestTab's missing-manifest path is api.manifestDiff() REJECTING (its .catch sets `error`, never `diff`). commands.rs's `manifest_diff_core` checks `workspace.join(\"izba.yml\").exists()` BEFORE calling `compute_diff` and, when it's genuinely absent, returns the exact string `no izba.yml found in workspace` — a stable sentinel distinct from a parse failure (`compute_diff` -> `Manifest::load_str` -> `.context(\"parsing izba.yml\")`), which also mentions izba.yml but is a different, honest-to-render error. ManifestTab keys its guidance panel on that sentinel (`missingManifest = error !== null && error.includes(\"no izba.yml found\")`), so a corrupt izba.yml now falls through to the raw error area instead of being mislabeled as absent. So this step's manifest_diff call never carries a `digest`, manifest_truth_oracle's `manifest_diff_seen` check stays false, and a `core: true` here would flip run_gui_journeys.py's `unreached_decisive` path even though the Actor did exactly what the step asked — a false product-negative, not a real one. The dom_expect oracle (which does not require a digest) still verifies this step from the rendered banner text.",
      "source": {
        "kind": "spec",
        "ref": "docs/superpowers/specs/2026-07-11-gui-manifest-dogfood-design.md §3.1 Manifest tab; ManifestTab.tsx MISSING_MANIFEST_HEADING/MISSING_MANIFEST_BODY; app/src-tauri/src/commands.rs manifest_diff_core"
      },
      "steps": [
        {
          "intent": "type {workspace} into the Workspace field and 'manifest-missing-demo' into the Name field (leave Image at its default), then click Create to create a new sandbox from this empty workspace",
          "expect": "a sandbox named manifest-missing-demo appears in the sandbox list"
        },
        {
          "intent": "open the manifest-missing-demo sandbox's detail view and click its Manifest tab",
          "expect": "the Manifest tab shows \"No izba.yml found in this sandbox's workspace.\" along with guidance to run 'izba export' or use Export to create one"
        }
      ]
    },
    {
      "journey_id": "manifest-managed-ahead-export",
      "modality": "gui",
      "tier": "smoke",
      "rationale": "The other half of the reconciliation story (complementing manifest-seeded-drift-promote's repo_ahead/Promote path): a user changes a sandbox's *live* settings through the app itself — here, editing the egress policy on the Policy tab (PolicyEditor.tsx) and clicking its Save button, which calls `policy_set_full` and mutates the managed truth (policy.yaml) without touching izba.yml — then opens the Manifest tab and must see the managed_ahead banner ('Live settings have drifted from izba.yml. Export to capture them.'), and Export must write that drift back into the file AND leave the tab showing the resulting in-sync state, not a stale managed_ahead re-render. `core: true` sits on the EXPORT step (moved here from the managed_ahead-view step, run-3 skeptic H3): a run-3 acceptance swarm reached the managed_ahead banner honestly (real `policy_set_full` -> real `manifest_diff` managed_ahead, ground-truth matched) but the journey ended there without ever clicking Export, so the flagship's actual round trip — Export writing the drift back into izba.yml and the tab settling on in-sync — went completely unverified while still reading as a clean pass. `ManifestTab.tsx`'s `doExport` re-calls `load()` after a successful export (mirroring `confirmPromote`, which already refetches after promote), so the Export click's own `manifest_diff` call is itself the decisive invoke this oracle grades: `run_gui_journeys.py`'s manifest_truth oracle always grades the LAST manifest_diff digest against `izba diff` ground truth, so putting `core: true` here forces the harness to treat the journey as incomplete (`unreached_decisive`) unless the Actor actually clicks Export and the post-export digest is the one ground-truthed — verifying the full write-back, not just the drift detection. The preceding Policy-tab save is NOT marked core — it never invokes manifest_diff, so marking it core would wrongly gate decisiveness on a step the oracle cannot verify; the managed_ahead-view step's own outcome is still checked by the ordinary end-of-journey dom_expect oracle against its banner text. This journey is also the one that ground-truths two GUI-manifest fixes: (1) the create-time manifest-base-seeding fix — `create_core` (app/src-tauri/src/commands.rs) mirrors `izba create`'s post-Create seeding (`write_manifest_base` calling the same `store::write_base`/`clear_review` + `EgressPolicyConfig::write_to` the CLI's `persist_policy_config` uses) for a sandbox created from a seeded workspace. Before that fix, `compute_diff`'s `base = managed.clone()` fallback (no `manifest.base.yaml` on disk for a GUI-created sandbox) made managed_ahead structurally unreachable here: the Policy-tab edit would have been misclassified as repo_ahead against the unchanged izba.yml, and this journey's decisive step would have asserted a banner the app could never show for a GUI-created sandbox. (2) the export-refetch fix — see above; before it, the Export click's `manifest_diff` call would still have carried the stale pre-export managed_ahead digest, and this decisive step could never observe an in-sync ground truth at all.",
      "source": {
        "kind": "spec",
        "ref": "docs/superpowers/specs/2026-07-11-gui-manifest-dogfood-design.md §3.1 Manifest tab; ManifestTab.tsx BANNER_TEXT.managed_ahead/in_sync + doExport; PolicyEditor.tsx save(); app/src-tauri/src/commands.rs write_manifest_base; dogfood-app-guide.md Policy / egress firewall + Manifest (izba.yml)"
      },
      "seed_files": {
        "izba.yml": "apiVersion: izba.dev/v1alpha1\nkind: Sandbox\nspec:\n  image: alpine:3.20\n"
      },
      "steps": [
        {
          "intent": "type {workspace} into the Workspace field, 'manifest-export-demo' into the Name field, and 'alpine:3.20' into the Image field, then click Create to create a new sandbox from this workspace",
          "expect": "a sandbox named manifest-export-demo appears in the sandbox list"
        },
        {
          "intent": "open the manifest-export-demo sandbox's detail view, click its Policy tab, click 'Add host', type 'example.com' into the new row's Host field, then click Save",
          "expect": "the Policy tab shows 'saved · reloaded'"
        },
        {
          "intent": "click the Manifest tab, then click Refresh to see whether the live settings have drifted from izba.yml",
          "expect": "the Manifest tab shows 'Live settings have drifted from izba.yml. Export to capture them.'"
        },
        {
          "intent": "click 'Export to izba.yml' on the Manifest tab",
          "expect": "the Manifest tab shows 'Exported to {workspace}/izba.yml', confirming the live policy change was captured back into the file, and the banner now reads 'In sync — izba.yml and managed settings match.' since the Export click's own diff refetch is now in sync",
          "core": true
        }
      ]
    }
  ]
}
