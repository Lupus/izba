# E2E CI (KVM + WHP) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Run izba's real-VM end-to-end suites in GitHub Actions: KVM/cloud-hypervisor on ubuntu-latest, OpenVMM/WHP on windows-latest — on main pushes, a weekly drift cron, and manual dispatch.

**Architecture:** A standalone `.github/workflows/e2e.yml` with boot-artifact jobs copied from `artifacts.yml` (identical cache keys, so the kernel restores from cache), one `linux-kvm` leg and one `windows-whp` leg. Prerequisite supply-chain work: sha256-pin the cloud-hypervisor/virtiofsd downloads, add a pinned passt build, and script the ttystorm M0 gate. The `artifacts.yml` Windows bundle switches from win-gnu cross to native MSVC so e2e validates the shipped toolchain.

**Tech Stack:** GitHub Actions (SHA-pinned actions), bash + PowerShell, cargo, KVM/cloud-hypervisor/passt/virtiofsd, OpenVMM/WHP.

**Spec:** `docs/superpowers/specs/2026-06-12-e2e-ci-design.md` (probe findings + decisions; read it first).

---

## Verified pins (computed 2026-06-12 — embed verbatim)

| What | Version / URL | sha256 |
| --- | --- | --- |
| cloud-hypervisor-static | v42.0, `https://github.com/cloud-hypervisor/cloud-hypervisor/releases/download/v42.0/cloud-hypervisor-static` | `537d1cbc1d4d3646099618f3b6f2b711116ad1ed8c8bc909a1a689417c7430aa` |
| virtiofsd release zip | v1.13.3 GitLab project upload (resolved via API) | `c79055af8189dcd3d942a16e5c165aa336aabbc47ea8e015c3a6cf9980ff73ab` |
| virtiofsd extracted binary | `target/x86_64-unknown-linux-musl/release/virtiofsd` inside the zip | `b3f7d24d7a530515b1a44b035f426c700553cb4f0cd14189051d54c0e6b6ef78` |
| passt tarball | tag `2026_05_26.038c51e`, `https://passt.top/passt/snapshot/passt-2026_05_26.038c51e.tar.gz` | `91df73b3d5a9bd6cac70087a5592414598d4be282e5d4f314dd03bf6d6e5e771` |

Pinned action SHAs (same as existing workflows): checkout `9f698171ed81b15d1823a05fc7211befd50c8ae0` (v6.0.3), cache `27d5ce7f107fe9357f9df03efb73ab90386fccae` (v5.0.5), upload-artifact `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (v7.0.1), download-artifact `3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (v8.0.1), Swatinem/rust-cache `23869a5bd66c73db3c0ac40331f3206eb23791dc` (v2.9.1).

## Facts you must not re-derive (verified against the code)

- Boot artifacts resolve via `$IZBA_KERNEL`/`$IZBA_INITRAMFS` env (both or neither — `crates/izba-core/src/artifacts.rs`), falling back to `<data>/artifacts/`. CI sets the env vars; no copying into data dirs.
- Tool binaries resolve via env override → `<exe dir>/libexec/` → `$PATH`: `$IZBA_OPENVMM` (`vmm/openvmm.rs::find_openvmm`), `$IZBA_MKFS_EROFS` (`image/erofs.rs`). CI uses the env overrides.
- `hack/fetch-artifacts.sh` exits 1 whenever its summary lists anything missing (passt instructions-only, boot artifacts) even after successfully installing CH+virtiofsd — CI must invoke it with `|| true` and assert the two binaries exist afterwards.
- `hack/build-mkfs-erofs-windows.sh --linux-only` outputs the native binary to `$HOME/.cache/izba/erofs-utils/build-linux/mkfs/mkfs.erofs` (NOT dist/). Deps: `curl tar make gcc autoconf automake libtoolize pkg-config patch`.
- passt's Makefile has a `static` target (`make static` = clean + all with `-static`); the binary to install is `passt` from the source root. izba needs only `passt` (not pasta), and probes `passt --help | grep vhost-user`.
- ttystorm builds with `cargo build --release -p izba-core --example ttystorm` → `target/release/examples/ttystorm[.exe]`. It needs a RUNNING sandbox and a running izbad (any `izba` CLI invocation auto-starts izbad; ttystorm itself never does).
- tty Tier-2: `cargo test -p izba-cli --features ttytests --test tty_e2e -- --test-threads=1` gated on `IZBA_TTY_E2E=1`; respects `IZBA_DATA_DIR`; uses `CARGO_BIN_EXE_izba` (the test-profile binary), so tool env overrides must be exported.
- The integration suite force-removes its tempdir sandboxes even on panic and prints the console tail in the panic message — there is nothing to upload for it; failure diagnostics come from stdout. `daemon_e2e`, `tty_e2e`, and the ttystorm gate use `IZBA_DATA_DIR`s under `$RUNNER_TEMP`, which IS uploadable.
- `validate-izba-windows.ps1` honors `IZBA_EXE`, `IZBA_IMAGE`, `IZBA_WS` env vars but hard-codes the console.log existence check at `$env:LOCALAPPDATA\izba\sandboxes\valid8\logs\console.log` — so do NOT set `IZBA_DATA_DIR` for that step (let it default), and upload `$env:LOCALAPPDATA\izba\sandboxes\**\logs\*` on failure.
- `workflow_dispatch` only works once the workflow exists on the default branch. During bring-up, add a TEMPORARY `push: branches: [feat/e2e-ci]` trigger and REMOVE it before merge (same trick as the previous CI project).
- This WSL2 host HAS working KVM — run local KVM verification with the sandbox disabled. Toolchain env for worktrees: `export RUSTUP_HOME=/home/kolkhovskiy/git/izba/.toolchain/rustup CARGO_HOME=/home/kolkhovskiy/git/izba/.toolchain/cargo PATH=/home/kolkhovskiy/git/izba/.toolchain/cargo/bin:$PATH`.
- Six local gates must stay green before every commit (see CLAUDE.md). For shell-script-only and YAML-only commits, `cargo fmt --check` + `cargo clippy` are unaffected but run the full set anyway before the final commit of each task; `bash -n <script>` for every shell edit.

---

### Task 1: sha256-pin cloud-hypervisor + virtiofsd in `hack/fetch-artifacts.sh`

**Files:**
- Modify: `hack/fetch-artifacts.sh` (CH section ~lines 82–104, virtiofsd `download_virtiofsd()` ~lines 119–152, passt section ~lines 199–207)

- [ ] **Step 1: Add pins and a verify helper near the top (after the `download_bin` helper)**

```bash
# Verify a file against an expected sha256; delete it and fail on mismatch.
verify_sha256() {
    local path="$1" want="$2" name="$3"
    local got
    got=$(sha256sum "$path" | cut -d' ' -f1)
    if [ "$got" != "$want" ]; then
        rm -f "$path"
        echo "  error: $name sha256 mismatch" >&2
        echo "    got:  $got" >&2
        echo "    want: $want" >&2
        return 1
    fi
    echo "  sha256 verified: $got"
}
```

- [ ] **Step 2: Pin cloud-hypervisor**

In the CH section, add below `CH_RELEASE_URL=`:

```bash
CH_SHA256="537d1cbc1d4d3646099618f3b6f2b711116ad1ed8c8bc909a1a689417c7430aa"
```

and change the install branch:

```bash
        download_bin "$CH_RELEASE_URL" "$BIN_DIR/cloud-hypervisor" "cloud-hypervisor"
        verify_sha256 "$BIN_DIR/cloud-hypervisor" "$CH_SHA256" "cloud-hypervisor"
        mark_ok "cloud-hypervisor"
```

- [ ] **Step 3: Pin virtiofsd (zip + extracted binary)**

Below `VIRTIOFSD_VERSION=` add:

```bash
# sha256 of the v1.13.3 release zip and of the static binary inside it.
# Bumping VIRTIOFSD_VERSION via env skips pin checks ONLY if you also
# override these (empty disables — intended for local experiments, never CI).
VIRTIOFSD_ZIP_SHA256="${VIRTIOFSD_ZIP_SHA256-c79055af8189dcd3d942a16e5c165aa336aabbc47ea8e015c3a6cf9980ff73ab}"
VIRTIOFSD_BIN_SHA256="${VIRTIOFSD_BIN_SHA256-b3f7d24d7a530515b1a44b035f426c700553cb4f0cd14189051d54c0e6b6ef78}"
```

Inside `download_virtiofsd()` after the zip download succeeds, insert:

```bash
    if [ -n "$VIRTIOFSD_ZIP_SHA256" ]; then
        verify_sha256 "$tmpd/virtiofsd.zip" "$VIRTIOFSD_ZIP_SHA256" "virtiofsd.zip" \
            || { rm -rf "$tmpd"; return 1; }
    fi
```

and after `bin=` resolution succeeds (before `install`):

```bash
    if [ -n "$VIRTIOFSD_BIN_SHA256" ]; then
        verify_sha256 "$bin" "$VIRTIOFSD_BIN_SHA256" "virtiofsd" \
            || { rm -rf "$tmpd"; return 1; }
    fi
```

- [ ] **Step 4: Point the passt guidance at the pinned build**

Replace the `echo` block recommending `https://passt.top/builds/latest/...` (unpinned!) with:

```bash
        echo "  → Build the pinned passt and install it (needs sudo):"
        echo "      hack/build-passt.sh"
        echo "      sudo install -m755 dist/passt-2026_05_26-static-x86_64 /usr/local/bin/passt"
        echo "      hash -r && passt --help | grep vhost-user   # verify"
```

(Task 2 creates that script; if executing tasks out of order is necessary, land Task 2 first.)

- [ ] **Step 5: Syntax-check and live-verify the download path**

```sh
bash -n hack/fetch-artifacts.sh
tmp=$(mktemp -d)
IZBA_BIN_DIR="$tmp" PATH=/usr/bin:/bin hack/fetch-artifacts.sh || true
test -x "$tmp/cloud-hypervisor" && test -x "$tmp/virtiofsd" && echo TOOLS-OK
rm -rf "$tmp"
```

Expected: both downloads print `sha256 verified: …`, then `TOOLS-OK`. (Needs real network; run unsandboxed.)

- [ ] **Step 6: Negative test — corrupt pin fails and deletes**

```sh
tmp=$(mktemp -d)
IZBA_BIN_DIR="$tmp" PATH=/usr/bin:/bin VIRTIOFSD_BIN_SHA256=deadbeef hack/fetch-artifacts.sh; echo "exit=$?"
test ! -e "$tmp/virtiofsd" && echo MISMATCH-DELETED-OK
rm -rf "$tmp"
```

Expected: `sha256 mismatch` with got/want lines, nonzero exit, `MISMATCH-DELETED-OK`.

- [ ] **Step 7: Run the six gates, then commit**

```bash
git add hack/fetch-artifacts.sh
git commit -m "feat(hack): sha256-pin cloud-hypervisor + virtiofsd downloads"
```

### Task 2: pinned passt build — `hack/build-passt.sh`

**Files:**
- Create: `hack/build-passt.sh` (mode 755)

- [ ] **Step 1: Write the script** (modeled on `hack/build-mke2fs.sh`)

```bash
#!/usr/bin/env bash
# Build a static passt (user-mode networking with --vhost-user) from a pinned
# source snapshot. Ubuntu 24.04's packaged passt (0.0~git20240220) predates
# vhost-user support, which izba requires (see hack/fetch-artifacts.sh §3).
#
# Output: dist/passt-2026_05_26-static-x86_64
# Install: sudo install -m755 dist/passt-2026_05_26-static-x86_64 /usr/local/bin/passt
set -euo pipefail

cd "$(dirname "$0")/.."

TAG="2026_05_26.038c51e"
SHORT="2026_05_26"
SHA256="91df73b3d5a9bd6cac70087a5592414598d4be282e5d4f314dd03bf6d6e5e771"
URL="https://passt.top/passt/snapshot/passt-${TAG}.tar.gz"

CACHE_DIR="${XDG_CACHE_HOME:-$HOME/.cache}/izba/passt"
SRC_DIR="$CACHE_DIR/passt-$TAG"
OUT="dist/passt-${SHORT}-static-x86_64"

MISSING=""
for tool in curl tar make gcc sha256sum file; do
    command -v "$tool" >/dev/null 2>&1 || MISSING="$MISSING $tool"
done
if [ -n "$MISSING" ]; then
    echo "error: missing tools:$MISSING" >&2
    echo "install with: sudo apt-get install -y curl tar make gcc coreutils file" >&2
    exit 1
fi

mkdir -p "$CACHE_DIR" dist
TARBALL="$CACHE_DIR/passt-$TAG.tar.gz"
if [ ! -f "$TARBALL" ]; then
    curl -fsSL -o "$TARBALL.part" "$URL"
    mv "$TARBALL.part" "$TARBALL"
fi
if ! echo "$SHA256  $TARBALL" | sha256sum -c - >/dev/null; then
    GOT=$(sha256sum "$TARBALL" | cut -d' ' -f1)
    rm -f "$TARBALL"
    echo "error: passt tarball sha256 mismatch (got $GOT, want $SHA256); deleted" >&2
    exit 1
fi

rm -rf "$SRC_DIR"
tar -xzf "$TARBALL" -C "$CACHE_DIR"

LOG="$CACHE_DIR/build.log"
if ! make -C "$SRC_DIR" -j"$(nproc)" static >"$LOG" 2>&1; then
    echo "error: passt build failed; last 30 lines of $LOG:" >&2
    tail -30 "$LOG" >&2
    exit 1
fi

install -m755 "$SRC_DIR/passt" "$OUT"
file "$OUT" | grep -q "statically linked" || {
    echo "error: $OUT is not statically linked: $(file "$OUT")" >&2
    exit 1
}
"./$OUT" --help 2>&1 | grep -q vhost-user || {
    echo "error: built passt does not advertise --vhost-user" >&2
    exit 1
}
echo "OK: $OUT ($(stat -c%s "$OUT") bytes)"
```

- [ ] **Step 2: Run it locally**

```sh
bash -n hack/build-passt.sh && chmod 755 hack/build-passt.sh && hack/build-passt.sh
```

Expected: `OK: dist/passt-2026_05_26-static-x86_64 (… bytes)`. (Network + compiler; run unsandboxed. If `make static` fails on the docs/man targets, the fallback is `make -C "$SRC_DIR" passt BASE_CFLAGS="-static"` — adjust the script and note it in a comment.)

- [ ] **Step 3: Six gates, commit**

```bash
git add hack/build-passt.sh
git commit -m "feat(hack): pinned static passt build (vhost-user-capable)"
```

### Task 3: ttystorm M0 gate script (Linux) — `hack/ci/ttystorm-gate.sh`

**Files:**
- Create: `hack/ci/ttystorm-gate.sh` (mode 755)

- [ ] **Step 1: Write the script**

```bash
#!/usr/bin/env bash
# M0 churn gate: izbad-path vsock churn must not kill the VM.
# (Exit criteria from docs/testing.md "vsock churn stressor"; the --direct
# control that DOES kill an unpatched OpenVMM is deliberately not run here.)
#
# Env:
#   IZBA_EXE      path to the izba binary            (default: izba on PATH)
#   TTYSTORM_EXE  path to the ttystorm example       (default: ttystorm on PATH)
#   IZBA_IMAGE    guest image                        (default: alpine:3.20)
#   IZBA_DATA_DIR honored by izba itself; set it to keep CI state collectable.
# Boot artifacts come from IZBA_KERNEL/IZBA_INITRAMFS or <data>/artifacts.
set -euo pipefail

IZBA_EXE="${IZBA_EXE:-izba}"
TTYSTORM_EXE="${TTYSTORM_EXE:-ttystorm}"
IZBA_IMAGE="${IZBA_IMAGE:-alpine:3.20}"
NAME="stormgate"
WS="$(mktemp -d)"

cleanup() {
    "$IZBA_EXE" rm --force "$NAME" >/dev/null 2>&1 || true
    rm -rf "$WS"
}
trap cleanup EXIT

echo "=== ttystorm gate: boot sandbox '$NAME' ==="
"$IZBA_EXE" run --image "$IZBA_IMAGE" --name "$NAME" "$WS" -- /bin/true

echo "=== ttystorm gate: floodfast 20 2048 ==="
"$TTYSTORM_EXE" "$NAME" floodfast 20 2048

echo "=== ttystorm gate: chop 30 256 ==="
"$TTYSTORM_EXE" "$NAME" chop 30 256

echo "=== ttystorm gate: VM survived? ==="
OUT="$("$IZBA_EXE" exec "$NAME" -- echo alive)"
if [ "$OUT" != "alive" ]; then
    echo "FAIL: exec after churn returned '$OUT' (VM dead or wedged)" >&2
    exit 1
fi
echo "PASS: VM alive after izbad-path churn"
```

- [ ] **Step 2: Verify locally against real KVM** (sandbox disabled; artifacts exist on this host)

```sh
bash -n hack/ci/ttystorm-gate.sh && chmod 755 hack/ci/ttystorm-gate.sh
cargo build --release -p izba-cli
cargo build --release -p izba-core --example ttystorm
export IZBA_KERNEL=$HOME/.local/share/izba/artifacts/vmlinux
export IZBA_INITRAMFS=$HOME/.local/share/izba/artifacts/initramfs.cpio.gz
IZBA_DATA_DIR=$(mktemp -d) \
IZBA_EXE=target/release/izba TTYSTORM_EXE=target/release/examples/ttystorm \
  hack/ci/ttystorm-gate.sh
```

Expected: the three `=== …` phases then `PASS: VM alive after izbad-path churn`.

- [ ] **Step 3: Six gates, commit**

```bash
git add hack/ci/ttystorm-gate.sh
git commit -m "feat(hack): scripted ttystorm M0 churn gate (linux)"
```

### Task 4: ttystorm M0 gate script (Windows) — `hack/ci/ttystorm-gate.ps1`

**Files:**
- Create: `hack/ci/ttystorm-gate.ps1`

- [ ] **Step 1: Write the script** (same flow as the .sh; PowerShell 7)

```powershell
# M0 churn gate (Windows/OpenVMM): izbad-path vsock churn must not kill the VM.
# Env: IZBA_EXE, TTYSTORM_EXE (required); IZBA_IMAGE (default alpine:3.20).
# Boot artifacts come from IZBA_KERNEL/IZBA_INITRAMFS or the default data dir.
$ErrorActionPreference = 'Stop'

$exe   = if ($env:IZBA_EXE)      { $env:IZBA_EXE }      else { 'izba' }
$storm = if ($env:TTYSTORM_EXE)  { $env:TTYSTORM_EXE }  else { 'ttystorm' }
$image = if ($env:IZBA_IMAGE)    { $env:IZBA_IMAGE }    else { 'alpine:3.20' }
$name  = 'stormgate'
$ws    = Join-Path ([System.IO.Path]::GetTempPath()) "stormgate-ws-$PID"

function Cleanup {
    & $exe rm --force $name 2>$null | Out-Null
    if (Test-Path $ws) { Remove-Item -Recurse -Force $ws -ErrorAction SilentlyContinue }
}

try {
    New-Item -ItemType Directory -Path $ws -Force | Out-Null

    Write-Host "=== ttystorm gate: boot sandbox '$name' ==="
    & $exe run --image $image --name $name $ws -- /bin/true
    if ($LASTEXITCODE -ne 0) { throw "izba run failed ($LASTEXITCODE)" }

    Write-Host "=== ttystorm gate: floodfast 20 2048 ==="
    & $storm $name floodfast 20 2048
    if ($LASTEXITCODE -ne 0) { throw "floodfast failed ($LASTEXITCODE)" }

    Write-Host "=== ttystorm gate: chop 30 256 ==="
    & $storm $name chop 30 256
    if ($LASTEXITCODE -ne 0) { throw "chop failed ($LASTEXITCODE)" }

    Write-Host "=== ttystorm gate: VM survived? ==="
    $out = (& $exe exec $name -- echo alive | Out-String).Trim()
    if ($LASTEXITCODE -ne 0 -or $out -ne 'alive') {
        throw "exec after churn returned '$out' exit=$LASTEXITCODE (VM dead or wedged)"
    }
    Write-Host "PASS: VM alive after izbad-path churn"
}
finally {
    Cleanup
}
```

- [ ] **Step 2: Static check only** (real verification happens in CI; the local Windows host route via `powershell.exe` interop is optional)

```sh
pwsh -NoProfile -Command '$t = Get-Content -Raw hack/ci/ttystorm-gate.ps1; $errs = $null; [System.Management.Automation.PSParser]::Tokenize($t, [ref]$errs) | Out-Null; if ($errs.Count) { $errs; exit 1 }; "PARSE-OK"'
```

(If `pwsh` is unavailable in WSL, run the same one-liner through `powershell.exe -NoProfile` interop, unsandboxed.) Expected: `PARSE-OK`.

- [ ] **Step 3: Six gates, commit**

```bash
git add hack/ci/ttystorm-gate.ps1
git commit -m "feat(hack): scripted ttystorm M0 churn gate (windows)"
```

### Task 5: the e2e workflow — `.github/workflows/e2e.yml`

**Files:**
- Create: `.github/workflows/e2e.yml`

- [ ] **Step 1: Write the workflow.** The kernel/mke2fs/initramfs jobs are verbatim copies from `artifacts.yml` (same cache key for vmlinux — that is load-bearing). Full file:

```yaml
name: E2E

on:
  push:
    branches: [main]
  schedule:
    - cron: '0 3 * * 1'   # weekly drift check, Mon 03:00 UTC (see spec)
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

permissions:
  contents: read

jobs:
  kernel:
    name: vmlinux (pinned 6.12.30)
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
      - name: Restore built vmlinux
        id: vmlinux
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: dist/vmlinux
          key: vmlinux-${{ hashFiles('hack/kernel.config', 'hack/build-kernel.sh') }}
      - name: Install kernel build deps
        if: steps.vmlinux.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends build-essential flex bison bc libelf-dev
      - name: Build kernel
        if: steps.vmlinux.outputs.cache-hit != 'true'
        run: hack/build-kernel.sh
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: vmlinux
          path: dist/vmlinux
          if-no-files-found: error

  mke2fs:
    name: static mke2fs (pinned e2fsprogs 1.47.2)
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
      - run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends musl-tools
      - run: hack/build-mke2fs.sh
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: mke2fs
          path: dist/mke2fs-*-static-x86_64
          if-no-files-found: error

  initramfs:
    name: initramfs (izba-init + embedded mke2fs)
    needs: mke2fs
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
      - run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends musl-tools cpio
      - uses: Swatinem/rust-cache@23869a5bd66c73db3c0ac40331f3206eb23791dc # v2.9.1
        with:
          prefix-key: initramfs
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: mke2fs
          path: dist/
      - name: Build initramfs with embedded mke2fs
        run: |
          files=(dist/mke2fs-*-static-x86_64)
          if [ "${#files[@]}" -ne 1 ] || [ ! -f "${files[0]}" ]; then
            echo "expected exactly 1 mke2fs binary, got: ${files[*]}" >&2
            exit 1
          fi
          chmod 755 "${files[0]}"
          IZBA_MKE2FS="${files[0]}" hack/build-initramfs.sh
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: initramfs
          path: dist/initramfs.cpio.gz
          if-no-files-found: error

  erofs-exe:
    name: mkfs.erofs.exe (pinned erofs-utils 1.9.1)
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
      - run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            curl tar make gcc autoconf automake libtool-bin pkg-config patch \
            gcc-mingw-w64-x86-64
      - run: hack/build-mkfs-erofs-windows.sh
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: e2e-mkfs-erofs-windows
          path: dist/mkfs.erofs.exe
          if-no-files-found: error

  linux-kvm:
    name: KVM e2e (integration + daemon + tty + ttystorm)
    needs: [kernel, initramfs]
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      IZBA_KERNEL: ${{ github.workspace }}/dist/vmlinux
      IZBA_INITRAMFS: ${{ github.workspace }}/dist/initramfs.cpio.gz
      IZBA_TEST_CACHE: /home/runner/.cache/izba-itest
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
      - name: Make /dev/kvm accessible
        run: |
          echo 'KERNEL=="kvm", GROUP="kvm", MODE="0666", OPTIONS+="static_node=kvm"' \
            | sudo tee /etc/udev/rules.d/99-kvm4all.rules
          sudo udevadm control --reload-rules
          sudo udevadm trigger --name-match=kvm || true
          ls -l /dev/kvm
          [ -r /dev/kvm ] && [ -w /dev/kvm ]
      - name: Restore runtime tools
        id: tools
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: |
            ~/.local/bin/cloud-hypervisor
            ~/.local/bin/virtiofsd
            ~/.local/bin/passt
            ~/.local/bin/mkfs.erofs
          key: e2e-tools-${{ hashFiles('hack/fetch-artifacts.sh', 'hack/build-passt.sh', 'hack/build-mkfs-erofs-windows.sh') }}
      - name: Build runtime tools (cache miss)
        if: steps.tools.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update
          sudo apt-get install -y --no-install-recommends \
            curl tar make gcc autoconf automake libtool-bin pkg-config patch
          mkdir -p "$HOME/.local/bin"
          IZBA_BIN_DIR="$HOME/.local/bin" hack/fetch-artifacts.sh || true
          hack/build-passt.sh
          install -m755 dist/passt-*-static-x86_64 "$HOME/.local/bin/passt"
          hack/build-mkfs-erofs-windows.sh --linux-only
          install -m755 "$HOME/.cache/izba/erofs-utils/build-linux/mkfs/mkfs.erofs" "$HOME/.local/bin/mkfs.erofs"
      - name: Assert tools present
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          for t in cloud-hypervisor virtiofsd passt mkfs.erofs; do
            command -v "$t" || { echo "missing $t" >&2; exit 1; }
          done
          passt --help | grep -q vhost-user
          mkfs.erofs --version
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: vmlinux
          path: dist/
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: initramfs
          path: dist/
      - name: Restore OCI test-image cache
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: /home/runner/.cache/izba-itest
          key: izba-itest-alpine-3.20
      - uses: Swatinem/rust-cache@23869a5bd66c73db3c0ac40331f3206eb23791dc # v2.9.1
        with:
          prefix-key: e2e-linux
      - name: Integration suite (15 tests, real KVM VMs)
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          IZBA_INTEGRATION=1 cargo test -p izba-core --test integration -- --test-threads=1 --nocapture
      - name: Daemon e2e (lifecycle suite)
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          IZBA_INTEGRATION=1 cargo test -p izba-cli --test daemon_e2e -- --test-threads=1 --nocapture
      - name: tty Tier-2 e2e
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          mkdir -p "$RUNNER_TEMP/tty-data"
          IZBA_TTY_E2E=1 IZBA_DATA_DIR="$RUNNER_TEMP/tty-data" \
            cargo test -p izba-cli --features ttytests --test tty_e2e -- --test-threads=1 --nocapture
      - name: ttystorm M0 churn gate
        run: |
          export PATH="$HOME/.local/bin:$PATH"
          cargo build --release -p izba-cli
          cargo build --release -p izba-core --example ttystorm
          mkdir -p "$RUNNER_TEMP/storm-data"
          IZBA_DATA_DIR="$RUNNER_TEMP/storm-data" \
          IZBA_EXE="$PWD/target/release/izba" \
          TTYSTORM_EXE="$PWD/target/release/examples/ttystorm" \
            hack/ci/ttystorm-gate.sh
      - name: Upload sandbox logs on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: linux-kvm-failure-logs
          path: |
            ${{ runner.temp }}/tty-data/**/logs/**
            ${{ runner.temp }}/storm-data/**/logs/**
          if-no-files-found: ignore

  windows-whp:
    name: WHP e2e (validate + tty + ttystorm)
    needs: [kernel, initramfs, erofs-exe]
    runs-on: windows-latest
    timeout-minutes: 60
    env:
      IZBA_KERNEL: ${{ github.workspace }}\dist\vmlinux
      IZBA_INITRAMFS: ${{ github.workspace }}\dist\initramfs.cpio.gz
      IZBA_OPENVMM: ${{ github.workspace }}\dist\openvmm.exe
      IZBA_MKFS_EROFS: ${{ github.workspace }}\dist\mkfs.erofs.exe
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: vmlinux
          path: dist/
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: initramfs
          path: dist/
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: e2e-mkfs-erofs-windows
          path: dist/
      - name: Restore pinned openvmm.exe
        id: openvmm
        uses: actions/cache@27d5ce7f107fe9357f9df03efb73ab90386fccae # v5.0.5
        with:
          path: dist/openvmm.exe
          key: openvmm-run-27240809751
      - name: Fetch pinned openvmm.exe (cache miss)
        if: steps.openvmm.outputs.cache-hit != 'true'
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: hack/fetch-openvmm.sh
      - uses: Swatinem/rust-cache@23869a5bd66c73db3c0ac40331f3206eb23791dc # v2.9.1
        with:
          prefix-key: e2e-windows
      - name: Build izba.exe + ttystorm.exe (native MSVC)
        run: |
          cargo build --release -p izba-cli
          cargo build --release -p izba-core --example ttystorm
      - name: Windows validation suite (21 checks)
        env:
          IZBA_EXE: ${{ github.workspace }}\target\release\izba.exe
        run: pwsh -NoProfile -File hack/spike/validate-izba-windows.ps1
      - name: tty Tier-2 e2e
        env:
          IZBA_TTY_E2E: '1'
          IZBA_DATA_DIR: ${{ runner.temp }}\tty-data
        run: cargo test -p izba-cli --features ttytests --test tty_e2e -- --test-threads=1 --nocapture
      - name: ttystorm M0 churn gate
        env:
          IZBA_EXE: ${{ github.workspace }}\target\release\izba.exe
          TTYSTORM_EXE: ${{ github.workspace }}\target\release\examples\ttystorm.exe
          IZBA_DATA_DIR: ${{ runner.temp }}\storm-data
        run: pwsh -NoProfile -File hack/ci/ttystorm-gate.ps1
      - name: Upload sandbox logs on failure
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: windows-whp-failure-logs
          path: |
            ${{ env.LOCALAPPDATA }}\izba\sandboxes\**\logs\**
            ${{ runner.temp }}\tty-data\**\logs\**
            ${{ runner.temp }}\storm-data\**\logs\**
          if-no-files-found: ignore
```

- [ ] **Step 2: Lint the YAML**

```sh
python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/e2e.yml')); print('YAML-OK')"
```

Expected: `YAML-OK`. Also re-check every `uses:` SHA against the pins table above.

- [ ] **Step 3: Six gates, commit**

```bash
git add .github/workflows/e2e.yml
git commit -m "feat(ci): e2e workflow — KVM + WHP suites on hosted runners"
```

### Task 6: artifacts.yml — Windows bundle goes native MSVC

**Files:**
- Modify: `.github/workflows/artifacts.yml` (the `izba-windows` job and the `manifest` VERSIONS block)

- [ ] **Step 1: Replace the `izba-windows` job** (was: ubuntu + win-gnu cross) with:

```yaml
  izba-windows:
    name: izba.exe bundle (native MSVC)
    needs: erofs-parity
    runs-on: windows-latest
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@9f698171ed81b15d1823a05fc7211befd50c8ae0 # v6.0.3
      - uses: Swatinem/rust-cache@23869a5bd66c73db3c0ac40331f3206eb23791dc # v2.9.1
        with:
          prefix-key: izba-windows-msvc
      - run: cargo build --release -p izba-cli
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: mkfs-erofs-windows
          path: stage/bin/libexec
      - name: Assemble installer-shaped bundle
        # Mirrors hack/stage-izba-windows.sh layout (bin/ + bin/libexec/)
        # minus openvmm.exe, which is fetched-not-built (out of scope; see
        # the design doc) and minus boot artifacts (separate artifacts here).
        # Native MSVC is the source-of-truth Windows toolchain (e2e CI spec,
        # decision 6); the win-gnu cross gates in ci.yml remain as
        # portability tripwires.
        run: |
          mkdir stage/bin -Force | Out-Null
          Copy-Item target/release/izba.exe stage/bin/
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: izba-windows-bundle
          path: stage/
          if-no-files-found: error
```

- [ ] **Step 2: Record the toolchain in VERSIONS** — in the `manifest` job's `Write SHA256SUMS + VERSIONS` step, add after the `erofs-utils` echo line:

```bash
            echo "izba-windows-toolchain: msvc (native)"
```

- [ ] **Step 3: Lint, six gates, commit**

```sh
python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/artifacts.yml')); print('YAML-OK')"
```

```bash
git add .github/workflows/artifacts.yml
git commit -m "feat(ci): publish Windows bundle from native MSVC build"
```

### Task 7: docs sync

**Files:**
- Modify: `docs/testing.md` (intro + §8), `hack/README.md` (script sections + env table), `CLAUDE.md` (Build & test section)

- [ ] **Step 1: `docs/testing.md`** — after the intro paragraph block (before §1), add:

```markdown
**CI coverage:** the same suites run on GitHub-hosted runners via
`.github/workflows/e2e.yml` (KVM on ubuntu-latest, OpenVMM/WHP on
windows-latest) on every push to main, weekly, and on manual dispatch — see
`docs/superpowers/specs/2026-06-12-e2e-ci-design.md`. This runbook remains
the local-development path.
```

In §8 ("The Windows port has no CI; …") replace that first sentence with: "The Windows port is CI-validated by the `windows-whp` job in `.github/workflows/e2e.yml`; this section is the manual spike-host route."

- [ ] **Step 2: `hack/README.md`** — add these two sections beside the `build-mke2fs.sh` one (match the file's existing heading style), and in the `fetch-artifacts.sh` section append the one-line note shown last:

```markdown
### build-passt.sh

Builds a static `passt` (user-mode networking) from a pinned source snapshot
(tag `2026_05_26.038c51e`, sha256-verified). Ubuntu 24.04's packaged passt
predates the `--vhost-user` mode izba requires. Output:
`dist/passt-2026_05_26-static-x86_64`; install with
`sudo install -m755 dist/passt-2026_05_26-static-x86_64 /usr/local/bin/passt`.

### ci/ttystorm-gate.sh / ci/ttystorm-gate.ps1

The scripted M0 vsock-churn gate used by `.github/workflows/e2e.yml`: boots a
throwaway sandbox, runs `ttystorm floodfast 20 2048` + `chop 30 256` through
izbad, and asserts the VM is still alive afterwards. Env: `IZBA_EXE`,
`TTYSTORM_EXE` (paths to the binaries), `IZBA_IMAGE` (default `alpine:3.20`).
```

```markdown
Downloads are sha256-pinned: cloud-hypervisor and virtiofsd are verified
after download and deleted on mismatch.
```

- [ ] **Step 3: `CLAUDE.md`** — in the Build & test section, after the daemon-e2e line, add:

```markdown
Both KVM suites + the Windows WHP validation also run in CI:
`.github/workflows/e2e.yml` (main pushes, weekly cron, manual dispatch).
```

- [ ] **Step 4: Six gates, commit**

```bash
git add docs/testing.md hack/README.md CLAUDE.md
git commit -m "docs: e2e CI coverage notes (testing.md, hack/README, CLAUDE.md)"
```

### Task 8: CI bring-up loop (iterate until green, then de-trigger)

This task is the controller's (main session's): push and steer fix subagents per failing job, budget ≥ 10 iterations per job.

- [ ] **Step 1: Add the TEMPORARY branch trigger** to `.github/workflows/e2e.yml` (workflow_dispatch does not work until the workflow is on the default branch):

```yaml
on:
  push:
    branches: [main, feat/e2e-ci]   # TEMP: feat/e2e-ci, remove before merge
```

Commit (`ci: TEMP branch trigger for e2e bring-up`), push `feat/e2e-ci`, and confirm with `git ls-remote origin feat/e2e-ci` that the push actually landed.

- [ ] **Step 2: Iterate until both legs are green.** For each red job: download the failing job's log (`gh run view <id> --log-failed`), diagnose, dispatch a fix subagent with the exact error text, verify the fix claim independently (`gh run view` after the next push — never trust "should work now"). Known likely first-round failures and their planned responses:
  - `boot_to_healthy_under_5s`-style timing flake → add an env knob to the test (e.g. `IZBA_BOOT_BUDGET_SECS`, default 5/10 locally) rather than weakening local budgets; set it only in e2e.yml.
  - openvmm fetch 403 (cross-repo `GITHUB_TOKEN`) → report to the user: an `OPENVMM_FETCH_TOKEN` repo secret (classic PAT, `public_repo` read) is needed; wire it as `GH_TOKEN: ${{ secrets.OPENVMM_FETCH_TOKEN || github.token }}`.
  - passt vhost-user handshake or consomme egress differences on runners → capture failure-log artifacts, inspect `passt.log`/`console.log` before changing anything.
- [ ] **Step 3: Prove the cached path.** After the first fully green run, push an empty commit (`git commit --allow-empty -m "ci: re-run e2e to verify warm caches"`) and confirm the second run is also green with kernel/tools cache hits (job timing collapses).
- [ ] **Step 4: Remove the TEMP trigger** (revert to `branches: [main]`), commit `ci: drop TEMP bring-up trigger`.
- [ ] **Step 5: Final whole-branch review** (final code reviewer subagent over `main..HEAD`), then hand the user the PR command (zsh `'''` multiline form) — or create it directly if the user has authorized PR creation this session.
